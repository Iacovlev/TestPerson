
spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/maria
    username: root
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  activemq:
    pool:
      max-connections: 10
server:
  port: 8443